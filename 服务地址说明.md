# HuLa-Server 服务地址说明

## 📊 Docker 服务状态

### ✅ 已启动的服务

| 服务名称 | 容器名称 | 状态 | 访问地址 | 说明 |
|:---|:---|:---|:---|:---|
| **MySQL** | mysql | ✅ 运行中 (healthy) | `localhost:13306` | 数据库服务 |
| **Redis** | redis | ✅ 运行中 | `localhost:16379` | 缓存服务 |
| **Nacos** | nacos-standalone-mysql | ✅ 运行中 | `http://localhost:8080` | 服务注册与配置中心 |
| **RocketMQ Namesrv** | rocketmq-namesrv | ✅ 运行中 | `localhost:9876` | 消息队列命名服务 |
| **RocketMQ Proxy** | rocketmq-proxy | ✅ 运行中 | `http://localhost:8081`<br>`http://localhost:8082` | 消息队列代理服务 |

### ⚠️ 需要处理的服务

| 服务名称 | 容器名称 | 状态 | 操作 |
|:---|:---|:---|:---|
| **RocketMQ Broker** | rocketmq-broker | ❌ 已退出 | 需要重启：`docker-compose restart rocketmq-broker` |

---

## 🔧 基础服务详细地址

### MySQL 数据库
```
地址: localhost:13306
用户名: root
密码: 123456
数据库: 
  - nacos (Nacos 配置数据库)
  - luohuo_dev (主业务数据库)
  - luohuo_im_01 (IM 业务数据库)
```

### Redis 缓存
```
地址: localhost:16379
密码: luo123456
数据库: 0-7 (根据配置使用不同数据库)
```

### Nacos 控制台
```
Web 控制台: http://localhost:8080/nacos
默认账号: nacos / nacos
命名空间: public (默认)
配置管理: 配置中心 -> 配置管理
服务管理: 服务管理 -> 服务列表
```

### RocketMQ
```
Namesrv 地址: localhost:9876
Broker 地址: localhost:10911 (需要重启 Broker)
Proxy 控制台: http://localhost:8081
Proxy Web: http://localhost:8082
```

---

## 🚀 应用服务地址（需要启动）

### 1. Gateway 网关服务
```
服务名称: luohuo-gateway-server
默认端口: 18760
访问地址: http://localhost:18760
API 文档: http://localhost:18760/doc.html
Swagger: http://localhost:18760/swagger-ui.html
```

**前端主要访问入口** ⭐
```
HTTP API: http://192.168.0.179:18760
WebSocket: ws://192.168.0.179:18760/ws/ws
```

### 2. OAuth 认证服务
```
服务名称: luohuo-oauth-server
默认端口: 18761 (需在 Nacos 配置中确认)
访问地址: http://localhost:18761
API 文档: http://localhost:18761/doc.html
路径前缀: /oauth
```

### 3. Base 基础服务
```
服务名称: luohuo-base-server
默认端口: 18762 (需在 Nacos 配置中确认)
访问地址: http://localhost:18762
API 文档: http://localhost:18762/doc.html
路径前缀: /base
```

### 4. IM 业务服务
```
服务名称: luohuo-im-server
默认端口: 18763 (需在 Nacos 配置中确认)
访问地址: http://localhost:18763
API 文档: http://localhost:18763/doc.html
路径前缀: /im
```

### 5. WebSocket 服务
```
服务名称: luohuo-ws-server
默认端口: 18764 (需在 Nacos 配置中确认)
访问地址: http://localhost:18764
WebSocket 路径: /ws/ws
路径前缀: /ws
```

### 6. AI 服务（可选）
```
服务名称: luohuo-ai-server
默认端口: 18765 (需在 Nacos 配置中确认)
访问地址: http://localhost:18765
API 文档: http://localhost:18765/doc.html
路径前缀: /ai
```

### 7. System 系统管理服务（可选）
```
服务名称: luohuo-system-server
默认端口: 18766 (需在 Nacos 配置中确认)
访问地址: http://localhost:18766
API 文档: http://localhost:18766/doc.html
路径前缀: /system
```

---

## 🌐 前端需要调用的地址

### 核心配置

在 HuLa 前端项目的 `src-tauri/configuration/local.yaml` 中配置：

```yaml
backend:
  # API 网关地址（前端主要访问入口）
  base_url: http://192.168.0.179:18760
  
  # WebSocket 连接地址（实时消息推送）
  ws_url: ws://192.168.0.179:18760/ws/ws
```

**重要说明：**
- 将 `192.168.0.179` 替换为您的实际服务器 IP 地址
- 如果前端和服务端在同一台机器，可以使用 `localhost` 或 `127.0.0.1`
- 如果前端在其他机器，需要使用服务器的局域网 IP 或公网 IP

### API 路由说明

前端通过网关访问后端服务，路由规则如下：

| 服务 | 网关路由前缀 | 完整路径示例 |
|:---|:---|:---|
| OAuth 认证 | `/oauth/**` | `http://192.168.0.179:18760/oauth/login` |
| Base 基础 | `/base/**` | `http://192.168.0.179:18760/base/tenant` |
| IM 业务 | `/im/**` | `http://192.168.0.179:18760/im/message/send` |
| WebSocket | `/ws/**` | `ws://192.168.0.179:18760/ws/ws` |
| AI 服务 | `/ai/**` | `http://192.168.0.179:18760/ai/chat` |
| System 系统 | `/system/**` | `http://192.168.0.179:18760/system/user` |

### WebSocket 连接

前端 WebSocket 连接地址：
```
ws://192.168.0.179:18760/ws/ws
```

**连接参数：**
- 需要在连接时携带认证 Token
- 连接成功后用于接收实时消息推送
- 支持断线重连机制

---

## 📝 服务启动检查清单

### 1. Docker 服务检查
```bash
# 检查所有容器状态
docker ps

# 检查 RocketMQ Broker 日志（如果未启动）
docker logs rocketmq-broker

# 重启 RocketMQ Broker
cd docs/install/docker
docker-compose restart rocketmq-broker
```

### 2. 数据库检查
```bash
# 检查 MySQL 连接
mysql -h localhost -P 13306 -uroot -p123456

# 检查数据库是否存在
show databases;
```

### 3. Nacos 配置检查
- 访问 http://localhost:8080/nacos
- 确认所有配置文件已导入
- 检查 Redis、MySQL、RocketMQ 配置中的 IP 地址是否正确

### 4. 应用服务检查
```bash
# 检查服务是否注册到 Nacos
# 访问 Nacos 控制台 -> 服务管理 -> 服务列表

# 检查服务健康状态
curl http://localhost:18760/actuator/health
```

---

## 🔍 常见问题

### 1. RocketMQ Broker 启动失败
**问题**: Broker 容器退出
**解决**:
```bash
# 检查日志
docker logs rocketmq-broker

# 检查 broker.conf 配置
# 确保 brokerIP1 设置为宿主机 IP (192.168.0.179)

# 重启服务
docker-compose restart rocketmq-broker
```

### 2. 前端无法连接后端
**问题**: 前端报连接错误
**解决**:
- 检查网关服务是否启动（端口 18760）
- 检查防火墙是否开放端口
- 确认前端配置中的 IP 地址正确
- 检查 CORS 配置（跨域问题）

### 3. WebSocket 连接失败
**问题**: WebSocket 无法建立连接
**解决**:
- 确认 WebSocket 服务已启动
- 检查网关路由配置
- 确认 Token 认证信息正确
- 检查网络连接

---

## 📚 相关文档

- [项目功能介绍](项目功能介绍.md)
- [服务端部署文档](docs/install/服务端部署文档.md)
- [前端项目指南](https://github.com/HuLaSpark/HuLa/blob/master/docs/project_guide.md)

---

**最后更新**: 2025-11-12
**当前服务器 IP**: 192.168.0.179

